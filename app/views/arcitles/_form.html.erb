<%= form_for(@arcitle) do |f| %>
  <% if @arcitle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@arcitle.errors.count, "error") %> prohibited this arcitle from being saved:</h2>

      <ul>
      <% @arcitle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
